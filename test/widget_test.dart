import 'package:flutter_test/flutter_test.dart';
import 'package:j_s_truck_park/main.dart';

void main() {
  test('MyApp can be constructed without starting production services', () {
    expect(MyApp(), isA<MyApp>());
  });
}
